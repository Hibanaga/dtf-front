import React, { FunctionComponent } from 'react';

import { Props } from './index';
import StyledComponent from './styles';

const ModuleLoginFormSectionSignInForm: FunctionComponent<Props> = ({ children }) => {
    return (
        <StyledComponent className="module-login-form-section-sign-in-form">

        </StyledComponent>
    );
};

export default ModuleLoginFormSectionSignInForm;
